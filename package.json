{
  "name": "pancakeswap-prediction-bot-js",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build": "babel src -d dist",
    "build:contracts": "truffle compile",
    "dev": "NODE_ENV=development npm start",
    "prod": "cross-env NODE_ENV=production npm start",
    "start": "npm run build:contracts && npm run build && node dist/index.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "MIT",
  "dependencies": {
    "dotenv": "^10.0.0",
    "truffle": "^5.4.17",
    "web3": "^1.6.0"
  },
  "devDependencies": {
    "@babel/cli": "^7.16.0",
    "@babel/core": "^7.16.0",
    "@babel/preset-env": "^7.16.0",
    "@naholyr/cross-env": "^1.0.0"
  }
}
